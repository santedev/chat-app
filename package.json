{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc --project tsconfig.json --watch",
    "start": "dotnet watch run --project restfulAPI/restfulAPI.csproj"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "10.9.2"
  },
  "dependencies": {
  }
}
